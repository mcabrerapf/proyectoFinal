#statical
  #logo
  nav.navbar.navbar-inverse
    .container
      .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
          span.sr-only Toggle navigation
          span.icon-bar 
          span.icon-bar 
          span.icon-bar 
        a.navbar-brand(href='/') \\Jammer//
        ul.nav.navbar-nav
          li
            a(href='/search') Find your musician!
          if (user)
            li
              a(href='/main-user/' + user._id)= user.username
            li
              a(href='/logout', style='text-align:right') Logout
          if (!user)
            li
              a(href='/sign-up') Sign Up!
            li
              a(href='/login') Login
